//UPDATE: Removed the longest path stuff, realized that it could lead to future errors if tested with way larger code


import java.util.ArrayList;
import java.util.Collections;

public class NewSolverWithoutRecursion {
    private static int empty = 0;


    public static ArrayList<int[]> solveMaze(String[][] maze) {
        ArrayList<int[]> path = new ArrayList<>();


        if (findRightPath(maze, 0, 0, path)){
            return path;
        }
        else {
            return null;
        }
    }

    public static boolean findRightPath(String[][] maze, int row, int col, ArrayList<int[]> path) {
        int rows = maze.length;
        int columns = maze[0].length;
        int fakeCoord = 100000;
        int[] f = new int[]{fakeCoord};

        ArrayList<int[]> q = new ArrayList<>();
        q.add(new int[]{fakeCoord, fakeCoord});
        for (int[] point : q) {
            System.out.print(("(" + point[0] + ", " + point[0] + ") ---> "));
        }


        if (row < 0 || row >= rows || col < 0 || col >= columns) {

            return false;

        }

        if (row == rows - 1 && col == columns - 1) {
            return true;
        }

        q.add(new int[]{fakeCoord, fakeCoord});
        for (row = 0; row < rows; row++) {
            for (col = 0; col < columns; col++) {


                    empty = 1;


                path.add(new int[]{row, col});
                if (empty == 1) {

                    path.set(0, f);
                    empty = 0;

                }

            }
        }
        if (path.equals(q)) {
            int i;
        }

        return true;
    }

    public static void printPath(ArrayList<int[]> path) {
        if (path != null) {
            System.out.println("Path:");
            for (int[] point : path) {
                System.out.print(("(" + point[0] + ", " + point[1] + ") ---> "));
            }


        }
        else {
            System.out.println("Invalid Maze. This is abysmal..");
        }
    }


}
